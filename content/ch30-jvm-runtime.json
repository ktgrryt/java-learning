{
  "id":"ch30","title":"Javaプログラムが動く仕組み","subtitle":"バイトコード・JVM・クラスローダー・JITを理解する","emoji":"⚙️",
  "lessons":[{
    "id":"30-1","title":"ソースコードから実行までをたどる",
    "explanation":"## Javaは二段階で実行される\n\n`javac Main.java` はソースをJVM命令であるバイトコード（`.class`）へ変換し、`java Main` がJVMを起動して実行します。クラスローダーは必要になったクラスを読み込み、検証・リンク・初期化します。JVMは最初から全コードを機械語にするのではなく、よく通る箇所をJITコンパイラで最適化します。\n\n## クラスローダーとモジュール\n\n`String.class.getClassLoader()` は `null` です。これは読み込まれていないのではなく、最上位のbootstrap loaderが担当した印です。アプリのクラスはapplication loaderが読みます。`Class` から名前、パッケージ、モジュール、親クラスなどの実行時メタデータを調べられます。\n\n```java\nClass<?> type = Class.forName(\"java.util.ArrayList\");\nSystem.out.println(type.getModule().getName()); // java.base\n```\n\nクラスパスはクラスを探す場所、モジュールパスはJPMSのモジュールを探す場所です。`ClassNotFoundException` や `NoClassDefFoundError` に遭遇したら、名前だけでなく実行時の配置と依存関係を確認します。",
    "samples":[{"caption":"実行中のクラスを観察する","code":"public class Main {\n    public static void main(String[] args) {\n        Class<?> type = Main.class;\n        System.out.println(type.getName());\n        System.out.println(type.getModule().isNamed() ? type.getModule().getName() : \"unnamed\");\n        System.out.println(type.getClassLoader().getClass().getSimpleName());\n        System.out.println(Runtime.version().feature() >= 21);\n    }\n}\n"}],
    "task":"クラス名を1つ読み、`Class.forName` で取得したクラスについて `class=単純名`、`module=モジュール名`、`loader=bootstrap/application` の3行を表示してください。名前のないモジュールは `unnamed`、クラスローダーがnullならbootstrap、それ以外はapplicationとします。",
    "starterCode":"import java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        String name = new Scanner(System.in).next();\n        // Class.forNameで調べる\n    }\n}\n",
    "visibleCases":[{"label":"String","stdin":"java.lang.String","expected":"class=String\nmodule=java.base\nloader=bootstrap"},{"label":"ArrayList","stdin":"java.util.ArrayList","expected":"class=ArrayList\nmodule=java.base\nloader=bootstrap"}],
    "hiddenCases":[{"label":"HashMap","stdin":"java.util.HashMap","expected":"class=HashMap\nmodule=java.base\nloader=bootstrap"},{"label":"自分のクラス","stdin":"Main","expected":"class=Main\nmodule=unnamed\nloader=application"}],
    "hints":["`Class<?> type = Class.forName(name);` で実行時のクラスを取得します。","モジュールは `type.getModule().isNamed()` を確認してから `getName()` を使います。","`type.getClassLoader() == null` ならbootstrapです。"],
    "solution":"import java.util.Scanner;\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        String name = new Scanner(System.in).next();\n        Class<?> type = Class.forName(name);\n        String module = type.getModule().isNamed() ? type.getModule().getName() : \"unnamed\";\n        String loader = type.getClassLoader() == null ? \"bootstrap\" : \"application\";\n        System.out.println(\"class=\" + type.getSimpleName());\n        System.out.println(\"module=\" + module);\n        System.out.println(\"loader=\" + loader);\n    }\n}\n",
    "extraTasks":[{"kind":"applied","task":"`arraylist`、`thread`、`main`のいずれかを読み、それぞれ `ArrayList.class`、`Thread.class`、`Main.class` を選びます。`package=パッケージ名` と `super=親クラスの単純名` を表示してください。名前のないパッケージは `unnamed` とします。","starterCode":"import java.util.*;\npublic class Main { public static void main(String[] args) { String key=new Scanner(System.in).next(); /* TODO */ } }\n","visibleCases":[{"label":"ArrayList","stdin":"arraylist","expected":"package=java.util\nsuper=AbstractList"},{"label":"Thread","stdin":"thread","expected":"package=java.lang\nsuper=Object"}],"hiddenCases":[{"label":"Main","stdin":"main","expected":"package=unnamed\nsuper=Object"}],"hints":["switch式で対応する `Class<?>` を選びます。","`type.getPackageName()` は名前のないパッケージでは空文字です。"],"solution":"import java.util.*;\npublic class Main { public static void main(String[] args) { String key=new Scanner(System.in).next(); Class<?> type=switch(key){case \"arraylist\"->ArrayList.class;case \"thread\"->Thread.class;default->Main.class;}; String p=type.getPackageName(); System.out.println(\"package=\"+(p.isEmpty()?\"unnamed\":p)); System.out.println(\"super=\"+type.getSuperclass().getSimpleName()); } }\n"}],
    "quiz":[{"question":"javacが通常生成するものは？","choices":["CPU固有の実行ファイル","JVM向けバイトコード","HTTPレスポンス","SQL"],"answer":1,"explanation":"`.class`にはJVMが解釈・コンパイルするバイトコードが入ります。"},{"question":"String.class.getClassLoader()がnullになる主な理由は？","choices":["Stringが未読込","bootstrap loaderが担当","GC済み","コンパイル失敗"],"answer":1,"explanation":"bootstrap loaderはJavaオブジェクトとして表されずnullになります。"},{"question":"JITの役割は？","choices":["ソースを保存","頻繁に実行されるコードを実行時に最適化","DBを作成","スレッドを必ず1本にする"],"answer":1,"explanation":"実行状況を使ってホットなコードを機械語へ最適化します。"},{"question":"ClassNotFoundExceptionで最初に確認するものは？","choices":["CSS","実行時のクラス名とclasspath/module path","画面サイズ","時刻だけ"],"answer":1,"explanation":"コンパイル時と実行時では依存クラスの探索場所が異なることがあります。"}]
  }]
}
