# パスワード保存ラボ

JDK標準のPBKDF2を使う最小例です。実運用では組織のセキュリティ要件に従い、
Argon2idなど利用環境で推奨される方式、パラメータ、秘密管理を選んでください。

```sh
javac PasswordHasher.java
java PasswordHasher
```

出力は「正しいパスワードは一致し、誤ったパスワードは一致しない」を示します。
保存文字列には方式、反復回数、salt、導出値が入ります。平文パスワードは入りません。
