-- キャンセルの再試行を追跡できるようにする移行。
--
-- 本番はこの移行を当てたあとも、まだ古い版のアプリが動いている時間がある
-- （入れ替えは一斉ではない）。だから expand（足すだけ）と contract（消す）を分け、
-- ここでは expand だけを行う。
--
-- 満たすこと:
--   * orders へ cancel_requested_at 列を足す（既存行にはNULLが入るので NOT NULL にしない）
--   * 再試行の記録を残す order_cancel_audit を作る
--     - order_id と idempotency_key の組を一意にする（同じ要求が二重に記録されない）
--     - attempts 列に何回下流を呼んだかを残す
--   * この移行では列やテーブルを DROP しない（古い版のアプリが読めなくなる）
--
-- TODO: 上の条件を満たすSQLを書く
